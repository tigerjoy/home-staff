Add support for entering invitation code if available so that
they can directly join the household skipping over the employee
addition process

When the user returns to the Onboarding Screen the saved houehold name is not re-populated back in the input box